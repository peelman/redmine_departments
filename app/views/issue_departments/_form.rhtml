<%= error_messages_for :department %>
Departments Total Count: <%= @departments.count %>
<table>
  <tr>
    <td>Choose Department:</td>
		<td><%= select ( 'department', 'name', @departments.map {|u| [u.name,u.id]} ) %></td>
  </tr>
  <tr>
    <td colspan="2">
      <%= submit_tag l(:button_add) %>
      <%= toggle_link l(:button_cancel), 'new-department-form'%>
    </td>
  </tr>
</table>
